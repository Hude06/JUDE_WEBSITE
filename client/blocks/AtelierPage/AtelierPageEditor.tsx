'use client';

export function AtelierPageEditor() {
  return (
    <p style={{ margin: 0, color: 'var(--muted-foreground)' }}>
      Atelier product page — no editable fields. Edit content directly in the component file.
    </p>
  );
}
