// Bumped on every framework release. Client sites receive this file via
// `npm run sync-framework`; fleet dashboards read it from /api/status to
// detect which sites are running behind.
export const FRAMEWORK_VERSION = '0.2.0';
